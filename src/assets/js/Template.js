class Template {
  userList(users) {
    return users.reduce(
      (acc, user) =>
        a +
        `
      <div>
        teste 2
      </div>
    `,
      ""
    );
  }
}

export default Template;
